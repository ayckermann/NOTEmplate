<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/notemplate_icon2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/notemplate_icon3"
        android:supportsRtl="true"
        android:theme="@style/Theme.NOTEmplate"
        tools:targetApi="31">

        <activity android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Register"
            android:exported="true">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".selectTemplate"
            android:theme="@style/PopUp">
        </activity>

        <activity android:name=".Template.templateNote"
            android:exported="true">
        </activity>

        <activity android:name=".Template.templateTodo"
            android:exported="true">
        </activity>

        <activity android:name=".Template.templateJadwal"
            android:exported="true">
        </activity>
    </application>

</manifest>